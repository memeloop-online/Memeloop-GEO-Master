{
  "name": "memeloop-geo",
  "version": "0.1.0",
  "private": true,
  "description": "Memeloop GEO monorepo",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=22.17.0",
    "pnpm": ">=10.17.1"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --if-present run dev",
    "build": "pnpm -r --if-present run build",
    "format": "prettier --write .github package.json pnpm-workspace.yaml compose.yaml README.md CONTRIBUTING.md && pnpm -r --if-present run format",
    "format:check": "prettier --check .github package.json pnpm-workspace.yaml compose.yaml README.md CONTRIBUTING.md && pnpm -r --if-present run format:check",
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm --filter @memeloop-geo/web exec tsc --build --pretty false",
    "test": "pnpm -r --if-present run test"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
