# 实施待办

只保留未完成工作和当前验收目标；完成项从本文件移除，细节写入 `WORKLOG.md`。

## 当前：按完整计划重新收口 W01

- [ ] 补齐登录会话、服务端身份推导和跨运营商/租户隔离测试。
- [ ] 收口主题、类型化 API 客户端和可恢复异步状态组件。
- [ ] 对现有 W02 未提交代码做保留/调整审计，不把旧范围直接标记完成。

## 下一步：W02 项目启动

- [ ] 完成 P01 草稿保存、资料/官网输入、资源与预算估算及启动任务。
- [ ] 持久化项目配置版本；启动返回 Operation，并在 P02 展示实际建库、基线和计划进度。
- [ ] 提供真实 API、迁移、OpenAPI、异常状态、自动化测试和演示数据。

## 主架构检查点

- [ ] W03/W05 建立有限、版本化的知识文档 fan-out 清单。
- [ ] W06/W08 建立文档 × 平台 fan-out 覆盖矩阵和独立发布分支。
- [ ] W09 建立发布证据 + AI 渠道测量 reduce、不可变周报和下一轮增量。

## 待补验证

- [ ] 容器镜像可用后，以 `GEO_TEST_DATABASE_URL` 在空 PostgreSQL 上执行嵌入迁移集成测试。

## 纵切目标

- [ ] 知识 → 基线 → 策略 → 内容 → 检查 → 账号池 → WordPress/Ghost 发布 → 验证 → 复测。
