# 实施待办

只保留未完成工作和当前验收目标；完成项从本文件移除，细节写入 `WORKLOG.md`。

## 当前：W02 项目启动

- [ ] 补齐项目时区、周报截止、文档范围、发布范围和版本化来源引用；区分草稿与启动校验。
- [ ] 将估算改为文档、文档×平台、AI 测量三类独立分母；未知数据明确返回未知。
- [ ] 原子创建配置版本、OptimizationCycle、两份未封存 manifest、Operation、Workflow 与 outbox，并支持刷新查回。
- [ ] 收口 P01 三步自动保存、文件/文本/官网/已有知识入口和 P02 持久化启动状态。

## W01 生产加固

- [ ] 完成所有 PostgreSQL repository 的事务级租户 scope 后启用 FORCE RLS，并使用非 bypass 角色实测。
- [ ] 将 SSE 改为持久化游标恢复，并在长连接期间重新校验会话与成员关系。

## 主架构检查点

- [ ] W03/W05 建立有限、版本化的知识文档 fan-out 清单。
- [ ] W06/W08 建立文档 × 平台 fan-out 覆盖矩阵和独立发布分支。
- [ ] W09 建立发布证据 + AI 渠道测量 reduce、不可变周报和下一轮增量。

## 待补验证

- [ ] 以 `GEO_TEST_DATABASE_URL` 在空 PostgreSQL 上执行迁移、原子启动、RLS 和重启恢复集成测试。

## 纵切目标

- [ ] 知识 → 基线 → 策略 → 内容 → 检查 → 账号池 → WordPress/Ghost 发布 → 验证 → 复测。
